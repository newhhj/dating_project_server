@page "/alertlist"
@layout MainLayout

@if (!this.IsInit)
{
    <div class="pageloading-box">
        데이터를 불러오는 중입니다.<br />
        잠시만 기다려 주세요.
    </div>
}
else
{
    <span class="label-title">신고 목록</span>
    <div class="context-box">
        <div class="filter-box">
            <input name="contentType" type="radio" id="radio01" value="0" checked="@(this.ContentType == 0)" @onclick="() => { this.ChangeContentType(0); }"/><label for="radio01">프로필</label>
            <input name="contentType" type="radio" id="radio02" value="1" checked="@(this.ContentType == 1)" @onclick="() => { this.ChangeContentType(1); }"/><label for="radio02">뿜뿜</label>
            <input name="contentType" type="radio" id="radio03" value="2" checked="@(this.ContentType == 2)" @onclick="() => { this.ChangeContentType(2); }"/><label for="radio03">댓글</label>
        </div>
        <table cellpadding="0" cellspacing="0">
            <thead>
                <tr>
                    <th>
                        제목
                    </th>
                    <th style="width: 200px;">
                        등록시간
                    </th>
                    <th style="width: 160px;">
                        -
                    </th>
                </tr>
            </thead>
            <tbody>
                @if (this.Items == null || this.Items.Length == 0)
                {
                    <tr>
                        <td colspan="3">
                            <div class="no-list">
                                표시할 목록이 없습니다.
                            </div>
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in this.Items)
                    {
                        <tr>
                            <td>
                                <span style="font-weight: bold; cursor: pointer;" @onclick="() => { this.OpenDetail(item); }">
                                    @item.Title
                                </span>
                            </td>
                            <td>
                                @item.CreateTime.ToString("yyyy.MM.dd")
                            </td>
                            <td>
                                <button style="background-color: #e11b1b; color: #ffffff;" @onclick="async () => { await this.RemoveItemAsync(item); }">삭제</button>
                            </td>
                        </tr>
                    }
                }
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3">
                        <div class="pager">
                            <btn @onclick="() => { this.Page--; this.MovePage(); }">
                                <i class="fas fa-chevron-left"></i>
                            </btn>
                            <input type="number" @bind="this.Page" @bind:event="oninput" @onkeydown='(e) => { if (e.Key == "Enter") this.MovePage(); }'/>
                            <span>/ @this.TotalPageCount.ToString("#,##0")</span>
                            <btn @onclick="() => { this.Page++; this.MovePage(); }">
                                <i class="fas fa-chevron-right"></i>
                            </btn>
                        </div>
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
}